name: Compression & Cleanup Automation
on:
  push:
    branches:
      - main
    paths:
      - '**/*.zip'
      - '**/*.7z'
      - '**/*.tar'
      - '**/*.gz'
      - '**/*.rar'
  schedule:
    - cron: '0 0 1,15 * *'
  workflow_dispatch:

jobs:
  extract-sync-delete:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Install Extraction Tools
        run: |
          sudo apt-get update -y
          sudo apt-get install -y p7zip-full rsync

      - name: Process, Overwrite, and Remove Archives
        run: |
          # Fallback Security: Safe temp workspace
          TEMP_PARENT="/tmp/ext_assets"
          mkdir -p "$TEMP_PARENT"

          # Logic Preservation: Find and process all archives
          find . -type f \( -name "*.zip" -o -name "*.7z" -o -name "*.rar" -o -name "*.tar" -o -name "*.gz" \) \
          -not -path "./.git/*" -print0 | while IFS= read -r -d '' ARCHIVE; do
            
            # Create a unique extraction folder
            EXTRACT_DIR="$TEMP_PARENT/$(basename "$ARCHIVE" | sed 's/[^a-zA-Z0-9]//g')"
            mkdir -p "$EXTRACT_DIR"
            
            # Extract and Overwrite (-aoa)
            if 7z x "$ARCHIVE" -o"$EXTRACT_DIR" -aoa; then
              TARGET_PATH=$(dirname "$ARCHIVE")
              
              # --- STRICT EXCLUSION LOGIC ---
              # -avI: Force overwrite
              # --exclude: Prevents specific files/folders from being replaced even if they exist in the ZIP
              rsync -avI \
                --exclude='.git/' \
                --exclude='.github/' \
                --exclude='node_modules/' \
                --exclude='package.json' \
                --exclude='package-lock.json' \
                --exclude='.env' \
                --exclude='README.md' \
                "$EXTRACT_DIR/" "$TARGET_PATH/"
              
              # Automation: Delete source archive after successful sync
              echo "Processed $ARCHIVE. Deleting source..."
              rm -f "$ARCHIVE"
            else
              echo "Error: Failed to extract $ARCHIVE."
            fi
            
            rm -rf "$EXTRACT_DIR"
          done
          rm -rf "$TEMP_PARENT"

      - name: Commit & Push Changes
        run: |
          git config --local user.email "hassanbiswas.github.io@gmail.com"
          git config --local user.name "Hassan Biswas"
          
          # Stage all changes (extracted assets + archive deletions)
          git add -A 
          
          if [ -n "$(git status --porcelain)" ]; then
            git commit -m "Automated Asset Sync & Strict Cleanup [skip ci]"
            git push origin main
          else
            echo "No changes detected."
          fi
          
